from django.apps import AppConfig

#LAFConfig is name of the app. It can be anything but it should start with uppercase
#lostandfound is name of the folder.

class LAFConfig(AppConfig):
	name='lostandfound'